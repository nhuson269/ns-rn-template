import type {AppEnv} from './env';

const envProd: AppEnv = {
  API_URL_TYPICODE: 'https://jsonplaceholder.typicode.com',
  API_URL_HEROKUAPP: 'https://api-nodejs-todolist.herokuapp.com',
};

export default envProd;
