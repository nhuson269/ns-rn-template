import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  statusBar: {},
  header: {
    height: 44,
    flexDirection: 'row',
    alignItems: 'center',
  },
  title: {
    position: 'absolute',
    fontSize: 16,
    fontWeight: 'bold',
    left: 16,
    right: 76,
  },
});
