import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
  },
  title: {
    position: "absolute",
    fontSize: 28,
    fontWeight: "bold",
  },
  activity: {
    position: "absolute",
  },
});
